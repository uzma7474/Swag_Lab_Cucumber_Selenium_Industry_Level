InventoryPage.feature

Feature: Inventory Page Validation

  Background:
    Given the user is logged in to SauceDemo

  @positive @INV001
  Scenario: Verify inventory page is displayed after successful login
    Then the inventory page should be displayed

  @positive @INV002
  Scenario: Verify inventory page title
    Then the inventory page title should be "Products"

  @positive @INV003
  Scenario: Verify inventory product list is displayed
    Then the inventory product list should be displayed

  @positive @INV004
  Scenario: Verify products are displayed
    Then products should be displayed on the inventory page

  @positive @INV005
  Scenario: Verify total number of products
    Then the inventory page should display 6 products
    
############################################################################################################
############################################################################################################

InventoryProducts.feature

Feature: Inventory Products

  Background:
    Given the user is logged in to SauceDemo
    And the inventory page is displayed


  @positive @INV006
  Scenario: Verify Sauce Labs Backpack
    Then the product "Sauce Labs Backpack" should be displayed
    And the price of product "Sauce Labs Backpack" should be "$29.99"


  @positive @INV007
  Scenario: Verify Sauce Labs Bike Light
    Then the product "Sauce Labs Bike Light" should be displayed
    And the price of product "Sauce Labs Bike Light" should be "$9.99"


  @positive @INV008
  Scenario: Verify Sauce Labs Bolt T-Shirt
    Then the product "Sauce Labs Bolt T-Shirt" should be displayed
    And the price of product "Sauce Labs Bolt T-Shirt" should be "$15.99"


  @positive @INV009
  Scenario: Verify Sauce Labs Fleece Jacket
    Then the product "Sauce Labs Fleece Jacket" should be displayed
    And the price of product "Sauce Labs Fleece Jacket" should be "$49.99"


  @positive @INV010
  Scenario: Verify Sauce Labs Onesie
    Then the product "Sauce Labs Onesie" should be displayed
    And the price of product "Sauce Labs Onesie" should be "$7.99"


  @positive @INV011
  Scenario: Verify Test.allTheThings T-Shirt
    Then the product "Test.allTheThings() T-Shirt" should be displayed
    And the price of product "Test.allTheThings() T-Shirt" should be "$15.99"
    
###########################################################################################################
###########################################################################################################
InventorySorting.feature

Feature: Inventory Product Sorting

  Background:
    Given the user is logged in to SauceDemo
    And the inventory page is displayed


  @positive @INV029
  Scenario: Sort products by name ascending
    When the user sorts products by "Name (A to Z)"
    Then the selected sort option should be "Name (A to Z)"


  @positive @INV030
  Scenario: Sort products by name descending
    When the user sorts products by "Name (Z to A)"
    Then the selected sort option should be "Name (Z to A)"


  @positive @INV031
  Scenario: Sort products by price low to high
    When the user sorts products by "Price (low to high)"
    Then the selected sort option should be "Price (low to high)"


  @positive @INV032
  Scenario: Sort products by price high to low
    When the user sorts products by "Price (high to low)"
    Then the selected sort option should be "Price (high to low)"


  @positive @INV033
  Scenario Outline: Verify inventory sorting options
    When the user sorts products by "<sortOption>"
    Then the selected sort option should be "<sortOption>"

    Examples:
      | sortOption          |
      | Name (A to Z)       |
      | Name (Z to A)       |
      | Price (low to high) |
      | Price (high to low) |
      
 
 
 
########################################################################################################### 
###########################################################################################################
InventoryCart.feature


@INVCart
Feature: Inventory Cart Operations

  Background:
    Given the user is logged in to SauceDemo
    And the inventory page is displayed


  @positive @INV020
  Scenario: Add one product to cart
    When the user adds "Sauce Labs Backpack" to the cart
    Then the cart badge should show 1


  @positive @INV021
  Scenario: Add two products to cart
    When the user adds "Sauce Labs Backpack" to the cart
    And the user adds "Sauce Labs Bike Light" to the cart
    Then the cart badge should show 2


  @positive @INV022
  Scenario: Add three products to cart
    When the user adds "Sauce Labs Backpack" to the cart
    And the user adds "Sauce Labs Bike Light" to the cart
    And the user adds "Sauce Labs Bolt T-Shirt" to the cart
    Then the cart badge should show 3


  @positive @INV023
  Scenario: Remove product from cart
    When the user adds "Sauce Labs Backpack" to the cart
    Then the cart badge should show 1
    When the user removes "Sauce Labs Backpack" from the cart
    Then the cart badge should not be displayed


  @positive @INV026
  Scenario: Open shopping cart
    When the user opens the shopping cart
    Then the shopping cart page should be displayed


  @positive @INV027
  Scenario: Add product and open cart
    When the user adds "Sauce Labs Backpack" to the cart
    And the user opens the shopping cart
    Then the shopping cart page should be displayed
    
    
    
    
###########################################################################################################    
###########################################################################################################

 # ============================================================
  # CASE INSENSITIVITY
  # ============================================================

  @positive @inventory @INV034
  Scenario: Verify product search validation is case insensitive
    Then the product "sauce labs backpack" should be displayed


  @positive @inventory @INV035
  Scenario: Verify product validation with leading and trailing spaces
    Then the product "  Sauce Labs Backpack  " should be displayed


  # ============================================================
  # MULTIPLE PRODUCT FLOW
  # ============================================================

  @positive @inventory @INV036
  Scenario: Add multiple products using business flow
    When the user adds "Sauce Labs Backpack" and "Sauce Labs Bike Light" and opens the cart
    Then the shopping cart page should be displayed


  @positive @inventory @INV037
  Scenario: Add first product and verify cart count
    When the user adds the first product to the cart
    Then the cart badge should be displayed
    And the cart badge should show 1


  # ============================================================
  # INVENTORY PAGE REFRESH
  # ============================================================

  @positive @inventory @INV038
  Scenario: Refresh inventory page
    When the user refreshes the inventory page
    Then the inventory page should be displayed


  @positive @inventory @INV039
  Scenario: Verify products remain displayed after page refresh
    When the user refreshes the inventory page
    Then products should be displayed on the inventory page


  # ============================================================
  # NEGATIVE SCENARIOS - PRODUCT
  # ============================================================

  @negative @inventory @INV040
  Scenario: Verify invalid product is not displayed
    Then the product "Invalid Product" should not be displayed


  @negative @inventory @INV041
  Scenario: Verify non-existing product is not displayed
    Then the product "iPhone 15" should not be displayed


  @negative @inventory @INV042
  Scenario: Verify blank product name is rejected
    When the user tries to verify product ""
    Then an illegal argument error should be raised


  @negative @inventory @INV043
  Scenario: Verify null product name is rejected
    When the user tries to verify a null product
    Then an illegal argument error should be raised


  # ============================================================
  # NEGATIVE SCENARIOS - PRICE
  # ============================================================

  @negative @inventory @INV044
  Scenario: Verify incorrect product price
    Then the price of product "Sauce Labs Backpack" should not be "$99.99"


  @negative @inventory @INV045
  Scenario: Verify price of non-existing product
    When the user tries to get the price of "Invalid Product"
    Then the product lookup should fail


  @negative @inventory @INV046
  Scenario: Verify blank product name while retrieving price
    When the user tries to get the price of ""
    Then an illegal argument error should be raised


  @negative @inventory @INV047
  Scenario: Verify null product name while retrieving price
    When the user tries to get the price of a null product
    Then an illegal argument error should be raised


  # ============================================================
  # NEGATIVE SCENARIOS - ADD TO CART
  # ============================================================

  @negative @inventory @INV048
  Scenario: Try to add invalid product to cart
    When the user tries to add "Invalid Product" to the cart
    Then the product lookup should fail


  @negative @inventory @INV049
  Scenario: Try to add blank product to cart
    When the user tries to add "" to the cart
    Then an illegal argument error should be raised


  @negative @inventory @INV050
  Scenario: Try to add null product to cart
    When the user tries to add a null product to the cart
    Then an illegal argument error should be raised


  # ============================================================
  # NEGATIVE SCENARIOS - REMOVE FROM CART
  # ============================================================

  @negative @inventory @INV051
  Scenario: Try to remove product that is not in cart
    When the user tries to remove "Sauce Labs Backpack" from the cart
    Then the product removal lookup should fail


  @negative @inventory @INV052
  Scenario: Try to remove invalid product from cart
    When the user tries to remove "Invalid Product" from the cart
    Then the product removal lookup should fail


  @negative @inventory @INV053
  Scenario: Try to remove blank product from cart
    When the user tries to remove "" from the cart
    Then an illegal argument error should be raised


  @negative @inventory @INV054
  Scenario: Try to remove null product from cart
    When the user tries to remove a null product from the cart
    Then an illegal argument error should be raised


  # ============================================================
  # NEGATIVE SCENARIOS - SORTING
  # ============================================================

  @negative @inventory @INV055
  Scenario: Try to sort using blank sort option
    When the user tries to sort products by ""
    Then an illegal argument error should be raised


  @negative @inventory @INV056
  Scenario: Try to sort using null sort option
    When the user tries to sort products using a null option
    Then an illegal argument error should be raised


  @negative @inventory @INV057
  Scenario: Try to sort using unsupported sort option
    When the user tries to sort products by "Invalid Sort"
    Then the sort operation should fail


  # ============================================================
  # NEGATIVE SCENARIOS - PRODUCT COUNT
  # ============================================================

  @negative @inventory @INV058
  Scenario: Verify incorrect product count
    Then the inventory page should not display 10 products


  @negative @inventory @INV059
  Scenario: Verify inventory is not empty
    Then the inventory product count should be greater than 0


  # ============================================================
  # NEGATIVE SCENARIOS - CART
  # ============================================================

  @negative @inventory @INV060
  Scenario: Verify cart badge is not displayed before adding product
    Then the cart badge should not be displayed


  @negative @inventory @INV061
  Scenario: Verify cart count is zero before adding product
    Then the cart badge count should be 0
    
    
    
############################################################################################################
###########################################################################################################
InventoryNegative.feature

@INVNegative
Feature: Inventory Negative Scenarios

  Background:
    Given the user is logged in to SauceDemo
    And the inventory page is displayed


  @negative @INV040
  Scenario: Verify invalid product is not displayed
    Then the product "Invalid Product" should not be displayed


  @negative @INV041
  Scenario: Verify non-existing product is not displayed
    Then the product "iPhone 15" should not be displayed


  @negative @INV044
  Scenario: Verify incorrect product price
    Then the price of product "Sauce Labs Backpack" should not be "$99.99"


  @negative @INV048
  Scenario: Try to add invalid product
    When the user tries to add "Invalid Product" to the cart
    Then the product lookup should fail


  @negative @INV049
  Scenario: Try to add blank product
    When the user tries to add "" to the cart
    Then an illegal argument error should be raised


  @negative @INV051
  Scenario: Try to remove product that is not in cart
    When the user tries to remove "Sauce Labs Backpack" from the cart
    Then the product removal lookup should fail


  @negative @INV055
  Scenario: Try to sort using blank option
    When the user tries to sort products by ""
    Then an illegal argument error should be raised


  @negative @INV057
  Scenario: Try to sort using invalid option
    When the user tries to sort products by "Invalid Sort"
    Then the sort operation should fail


  @negative @INV058
  Scenario: Verify incorrect product count
    Then the inventory page should not display 10 products
    

   


############################################################################################################
###########################################################################################################

Feature: SauceDemo Inventory Page

  Background:
    Given the user is logged in to SauceDemo
    And the inventory page is displayed


  # ============================================================
  # INVENTORY PAGE - POSITIVE SCENARIOS
  # ============================================================

  @positive @inventory @INV001
  Scenario: Verify inventory page is displayed after successful login
    Then the inventory page should be displayed


  @positive @inventory @INV002
  Scenario: Verify inventory page title
    Then the inventory page title should be "Products"


  @positive @inventory @INV003
  Scenario: Verify inventory product list is displayed
    Then the inventory product list should be displayed


  @positive @inventory @INV004
  Scenario: Verify products are displayed on inventory page
    Then products should be displayed on the inventory page


  @positive @inventory @INV005
  Scenario: Verify total number of products
    Then the inventory page should display 6 products


  # ============================================================
  # PRODUCT NAME SCENARIOS
  # ============================================================

  @positive @inventory @INV006
  Scenario: Verify Sauce Labs Backpack is displayed
    Then the product "Sauce Labs Backpack" should be displayed


  @positive @inventory @INV007
  Scenario: Verify Sauce Labs Bike Light is displayed
    Then the product "Sauce Labs Bike Light" should be displayed


  @positive @inventory @INV008
  Scenario: Verify Sauce Labs Bolt T-Shirt is displayed
    Then the product "Sauce Labs Bolt T-Shirt" should be displayed


  @positive @inventory @INV009
  Scenario: Verify Sauce Labs Fleece Jacket is displayed
    Then the product "Sauce Labs Fleece Jacket" should be displayed


  @positive @inventory @INV010
  Scenario: Verify Sauce Labs Onesie is displayed
    Then the product "Sauce Labs Onesie" should be displayed


  @positive @inventory @INV011
  Scenario: Verify Test.allTheThings() T-Shirt is displayed
    Then the product "Test.allTheThings() T-Shirt" should be displayed


  # ============================================================
  # PRODUCT PRICE SCENARIOS
  # ============================================================

  @positive @inventory @INV012
  Scenario: Verify Sauce Labs Backpack price
    Then the price of product "Sauce Labs Backpack" should be "$29.99"


  @positive @inventory @INV013
  Scenario: Verify Sauce Labs Bike Light price
    Then the price of product "Sauce Labs Bike Light" should be "$9.99"


  @positive @inventory @INV014
  Scenario: Verify Sauce Labs Bolt T-Shirt price
    Then the price of product "Sauce Labs Bolt T-Shirt" should be "$15.99"


  @positive @inventory @INV015
  Scenario: Verify Sauce Labs Fleece Jacket price
    Then the price of product "Sauce Labs Fleece Jacket" should be "$49.99"


  @positive @inventory @INV016
  Scenario: Verify Sauce Labs Onesie price
    Then the price of product "Sauce Labs Onesie" should be "$7.99"


  @positive @inventory @INV017
  Scenario: Verify Test.allTheThings() T-Shirt price
    Then the price of product "Test.allTheThings() T-Shirt" should be "$15.99"


  # ============================================================
  # CART DISPLAY SCENARIOS
  # ============================================================

  @positive @inventory @INV018
  Scenario: Verify shopping cart is displayed
    Then the shopping cart should be displayed


  @positive @inventory @INV019
  Scenario: Verify cart badge is not displayed before adding product
    Then the cart badge should not be displayed


  @positive @inventory @INV020
  Scenario: Add one product to cart
    When the user adds "Sauce Labs Backpack" to the cart
    Then the cart badge should show 1


  @positive @inventory @INV021
  Scenario: Add two different products to cart
    When the user adds "Sauce Labs Backpack" to the cart
    And the user adds "Sauce Labs Bike Light" to the cart
    Then the cart badge should show 2


  @positive @inventory @INV022
  Scenario: Add three products to cart
    When the user adds "Sauce Labs Backpack" to the cart
    And the user adds "Sauce Labs Bike Light" to the cart
    And the user adds "Sauce Labs Bolt T-Shirt" to the cart
    Then the cart badge should show 3


  @positive @inventory @INV023
  Scenario: Remove product from cart
    When the user adds "Sauce Labs Backpack" to the cart
    Then the cart badge should show 1
    When the user removes "Sauce Labs Backpack" from the cart
    Then the cart badge should not be displayed


  @positive @inventory @INV024
  Scenario: Add and remove one product
    When the user adds "Sauce Labs Backpack" to the cart
    And the user removes "Sauce Labs Backpack" from the cart
    Then the cart badge should not be displayed


  # ============================================================
  # FIRST PRODUCT SCENARIOS
  # ============================================================

  @positive @inventory @INV025
  Scenario: Add first product to cart
    When the user adds the first product to the cart
    Then the cart badge should show 1


  # ============================================================
  # CART NAVIGATION
  # ============================================================

  @positive @inventory @INV026
  Scenario: Navigate to shopping cart
    When the user opens the shopping cart
    Then the shopping cart page should be displayed


  @positive @inventory @INV027
  Scenario: Add product and open shopping cart
    When the user adds "Sauce Labs Backpack" to the cart
    And the user opens the shopping cart
    Then the shopping cart page should be displayed


  @positive @inventory @INV028
  Scenario: Add multiple products and open shopping cart
    When the user adds "Sauce Labs Backpack" to the cart
    And the user adds "Sauce Labs Bike Light" to the cart
    And the user opens the shopping cart
    Then the shopping cart page should be displayed


  # ============================================================
  # SORTING - POSITIVE
  # ============================================================

  @positive @inventory @INV029
  Scenario: Sort products by name ascending
    When the user sorts products by "Name (A to Z)"
    Then the selected sort option should be "Name (A to Z)"


  @positive @inventory @INV030
  Scenario: Sort products by name descending
    When the user sorts products by "Name (Z to A)"
    Then the selected sort option should be "Name (Z to A)"


  @positive @inventory @INV031
  Scenario: Sort products by price low to high
    When the user sorts products by "Price (low to high)"
    Then the selected sort option should be "Price (low to high)"


  @positive @inventory @INV032
  Scenario: Sort products by price high to low
    When the user sorts products by "Price (high to low)"
    Then the selected sort option should be "Price (high to low)"


  @positive @inventory @INV033
  Scenario Outline: Verify all inventory sorting options
    When the user sorts products by "<sortOption>"
    Then the selected sort option should be "<sortOption>"

    Examples:
      | sortOption           |
      | Name (A to Z)        |
      | Name (Z to A)        |
      | Price (low to high)  |
      | Price (high to low) |


  # ============================================================
  # CASE INSENSITIVITY
  # ============================================================

  @positive @inventory @INV034
  Scenario: Verify product search validation is case insensitive
    Then the product "sauce labs backpack" should be displayed


  @positive @inventory @INV035
  Scenario: Verify product validation with leading and trailing spaces
    Then the product "  Sauce Labs Backpack  " should be displayed


  # ============================================================
  # MULTIPLE PRODUCT FLOW
  # ============================================================

  @positive @inventory @INV036
  Scenario: Add multiple products using business flow
    When the user adds "Sauce Labs Backpack" and "Sauce Labs Bike Light" and opens the cart
    Then the shopping cart page should be displayed


  @positive @inventory @INV037
  Scenario: Add first product and verify cart count
    When the user adds the first product to the cart
    Then the cart badge should be displayed
    And the cart badge should show 1


  # ============================================================
  # INVENTORY PAGE REFRESH
  # ============================================================

  @positive @inventory @INV038
  Scenario: Refresh inventory page
    When the user refreshes the inventory page
    Then the inventory page should be displayed


  @positive @inventory @INV039
  Scenario: Verify products remain displayed after page refresh
    When the user refreshes the inventory page
    Then products should be displayed on the inventory page


  # ============================================================
  # NEGATIVE SCENARIOS - PRODUCT
  # ============================================================

  @negative @inventory @INV040
  Scenario: Verify invalid product is not displayed
    Then the product "Invalid Product" should not be displayed


  @negative @inventory @INV041
  Scenario: Verify non-existing product is not displayed
    Then the product "iPhone 15" should not be displayed


  @negative @inventory @INV042
  Scenario: Verify blank product name is rejected
    When the user tries to verify product ""
    Then an illegal argument error should be raised


  @negative @inventory @INV043
  Scenario: Verify null product name is rejected
    When the user tries to verify a null product
    Then an illegal argument error should be raised


  # ============================================================
  # NEGATIVE SCENARIOS - PRICE
  # ============================================================

  @negative @inventory @INV044
  Scenario: Verify incorrect product price
    Then the price of product "Sauce Labs Backpack" should not be "$99.99"


  @negative @inventory @INV045
  Scenario: Verify price of non-existing product
    When the user tries to get the price of "Invalid Product"
    Then the product lookup should fail


  @negative @inventory @INV046
  Scenario: Verify blank product name while retrieving price
    When the user tries to get the price of ""
    Then an illegal argument error should be raised


  @negative @inventory @INV047
  Scenario: Verify null product name while retrieving price
    When the user tries to get the price of a null product
    Then an illegal argument error should be raised


  # ============================================================
  # NEGATIVE SCENARIOS - ADD TO CART
  # ============================================================

  @negative @inventory @INV048
  Scenario: Try to add invalid product to cart
    When the user tries to add "Invalid Product" to the cart
    Then the product lookup should fail


  @negative @inventory @INV049
  Scenario: Try to add blank product to cart
    When the user tries to add "" to the cart
    Then an illegal argument error should be raised


  @negative @inventory @INV050
  Scenario: Try to add null product to cart
    When the user tries to add a null product to the cart
    Then an illegal argument error should be raised


  # ============================================================
  # NEGATIVE SCENARIOS - REMOVE FROM CART
  # ============================================================

  @negative @inventory @INV051
  Scenario: Try to remove product that is not in cart
    When the user tries to remove "Sauce Labs Backpack" from the cart
    Then the product removal lookup should fail


  @negative @inventory @INV052
  Scenario: Try to remove invalid product from cart
    When the user tries to remove "Invalid Product" from the cart
    Then the product removal lookup should fail


  @negative @inventory @INV053
  Scenario: Try to remove blank product from cart
    When the user tries to remove "" from the cart
    Then an illegal argument error should be raised


  @negative @inventory @INV054
  Scenario: Try to remove null product from cart
    When the user tries to remove a null product from the cart
    Then an illegal argument error should be raised


  # ============================================================
  # NEGATIVE SCENARIOS - SORTING
  # ============================================================

  @negative @inventory @INV055
  Scenario: Try to sort using blank sort option
    When the user tries to sort products by ""
    Then an illegal argument error should be raised


  @negative @inventory @INV056
  Scenario: Try to sort using null sort option
    When the user tries to sort products using a null option
    Then an illegal argument error should be raised


  @negative @inventory @INV057
  Scenario: Try to sort using unsupported sort option
    When the user tries to sort products by "Invalid Sort"
    Then the sort operation should fail


  # ============================================================
  # NEGATIVE SCENARIOS - PRODUCT COUNT
  # ============================================================

  @negative @inventory @INV058
  Scenario: Verify incorrect product count
    Then the inventory page should not display 10 products


  @negative @inventory @INV059
  Scenario: Verify inventory is not empty
    Then the inventory product count should be greater than 0


  # ============================================================
  # NEGATIVE SCENARIOS - CART
  # ============================================================

  @negative @inventory @INV060
  Scenario: Verify cart badge is not displayed before adding product
    Then the cart badge should not be displayed


  @negative @inventory @INV061
  Scenario: Verify cart count is zero before adding product
    Then the cart badge count should be 0





#######################################################################################################
##########################################################################################################
 

Feature: SauceDemo Inventory Page

  Background:
    Given the user is logged in as "standard_user"


  @positive @INV001
  Scenario: Verify inventory page is displayed
    Then the inventory page should be displayed


  @positive @INV002
  Scenario: Verify inventory page title
    Then the inventory page title should be "Products"


  @positive @INV003
  Scenario: Verify products are displayed
    Then the product list should be displayed


  @positive @INV004
  Scenario: Verify all products are displayed
    Then the inventory should contain 6 products


  @positive @INV005
  Scenario: Verify Sauce Labs Backpack is displayed
    Then the product "Sauce Labs Backpack" should be displayed


  @positive @INV006
  Scenario: Verify Sauce Labs Bike Light is displayed
    Then the product "Sauce Labs Bike Light" should be displayed


  @positive @INV007
  Scenario: Verify Sauce Labs Bolt T-Shirt is displayed
    Then the product "Sauce Labs Bolt T-Shirt" should be displayed


  @positive @INV008
  Scenario: Verify Sauce Labs Fleece Jacket is displayed
    Then the product "Sauce Labs Fleece Jacket" should be displayed


  @positive @INV009
  Scenario: Verify Sauce Labs Onesie is displayed
    Then the product "Sauce Labs Onesie" should be displayed


  @positive @INV010
  Scenario: Verify Test.allTheThings T-Shirt is displayed
    Then the product "Test.allTheThings() T-Shirt (Red)" should be displayed


  @positive @INV011
  Scenario: Sort products from A to Z
    When the user sorts products by "Name (A to Z)"
    Then products should be sorted in ascending alphabetical order


  @positive @INV012
  Scenario: Sort products from Z to A
    When the user sorts products by "Name (Z to A)"
    Then products should be sorted in descending alphabetical order


  @positive @INV013
  Scenario: Sort products from low to high price
    When the user sorts products by "Price (low to high)"
    Then products should be sorted by price from low to high


  @positive @INV014
  Scenario: Sort products from high to low price
    When the user sorts products by "Price (high to low)"
    Then products should be sorted by price from high to low


  @positive @INV015
  Scenario: Add one product to cart
    When the user adds "Sauce Labs Backpack" to the cart
    Then the cart badge should display "1"


  @positive @INV016
  Scenario: Add multiple products to cart
    When the user adds "Sauce Labs Backpack" to the cart
    And the user adds "Sauce Labs Bike Light" to the cart
    Then the cart badge should display "2"


  @positive @INV017
  Scenario: Remove product from inventory page
    When the user adds "Sauce Labs Backpack" to the cart
    And the user removes "Sauce Labs Backpack" from the cart
    Then the cart badge should not be displayed


  @positive @INV018
  Scenario: Open product details from inventory
    When the user clicks on "Sauce Labs Backpack"
    Then the product details page should be displayed


  @positive @INV019
  Scenario: Verify product image is displayed
    Then the product "Sauce Labs Backpack" image should be displayed


  @positive @INV020
  Scenario: Verify product price is displayed
    Then the product "Sauce Labs Backpack" price should be displayed


  @positive @INV021
  Scenario: Verify product description is displayed
    Then the product "Sauce Labs Backpack" description should be displayed


  @negative @INV022
  Scenario: Verify invalid product is not displayed
    Then the product "Invalid Product" should not be displayed


  @negative @INV023
  Scenario: Verify invalid sort option cannot be selected
    When the user attempts to select an invalid product sort option
    Then the invalid sort option should not be accepted
    
#=====================================================================================================

```java
package pages;

import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.NoSuchElementException;
import org.openqa.selenium.StaleElementReferenceException;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.FindBy;
import org.openqa.selenium.support.PageFactory;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import base.BasePage;
import driver.DriverManager;
import utils.WaitUtils;

public class CartPage extends BasePage {

    private static final Logger log = LoggerFactory.getLogger(CartPage.class);

    // ============================================================
    // Page Locators
    // ============================================================

    @FindBy(className = "title")
    private WebElement pageTitle;

    @FindBy(css = "div.cart_list")
    private WebElement cartList;

    /*
     * Each product in cart:
     * <div class="cart_item" data-test="inventory-item">
     */
    @FindBy(css = "div.cart_item[data-test='inventory-item']")
    private List<WebElement> cartItems;

    @FindBy(css = ".shopping_cart_link")
    private WebElement shoppingCartIcon;

    @FindBy(css = ".shopping_cart_badge")
    private WebElement shoppingCartBadge;

    @FindBy(id = "continue-shopping")
    private WebElement continueShoppingButton;

    @FindBy(id = "checkout")
    private WebElement checkoutButton;

    // ============================================================
    // Constructor
    // ============================================================

    public CartPage() {
        super();

        if (DriverManager.getDriver() == null) {
            throw new IllegalStateException(
                    "WebDriver is not initialized before creating CartPage");
        }

        PageFactory.initElements(driver, this);

        log.info("CartPage initialized successfully");
    }

    // ============================================================
    // Page Navigation
    // ============================================================

    /**
     * Opens the SauceDemo cart page.
     */
    public void open() {
        String cartUrl = EnvironmentManager.getBaseUrl() + "/cart.html";

        log.info("Opening Cart page: {}", cartUrl);

        navigateTo(cartUrl);

        log.info("Cart page opened successfully");
    }

    /**
     * Returns current browser URL.
     */
    public String getCurrentUrl() {
        String currentUrl = driver.getCurrentUrl();

        log.debug("Current URL: {}", currentUrl);

        return currentUrl;
    }

    // ============================================================
    // Page Validation
    // ============================================================

    /**
     * Checks whether cart page title is displayed.
     */
    public boolean isPageTitleDisplayed() {

        try {
            boolean displayed = pageTitle.isDisplayed();

            log.debug("Cart page title displayed: {}", displayed);

            return displayed;

        } catch (NoSuchElementException | StaleElementReferenceException e) {

            log.warn("Cart page title is not displayed");

            return false;
        }
    }

    /**
     * Returns cart page title text.
     */
    public String getPageTitle() {

        String title = pageTitle.getText().trim();

        log.debug("Cart page title: {}", title);

        return title;
    }

    /**
     * Checks whether cart list is displayed.
     */
    public boolean isCartListDisplayed() {

        try {

            boolean displayed = cartList.isDisplayed();

            log.debug("Cart list displayed: {}", displayed);

            return displayed;

        } catch (NoSuchElementException | StaleElementReferenceException e) {

            log.warn("Cart list is not displayed");

            return false;
        }
    }

    // ============================================================
    // Cart Items
    // ============================================================

    /**
     * Returns number of products currently displayed in cart.
     */
    public int getCartItemCount() {

        int count = driver.findElements(
                By.cssSelector("div.cart_item[data-test='inventory-item']")
        ).size();

        log.info("Number of products currently in cart: {}", count);

        return count;
    }

    /**
     * Checks whether cart is empty.
     */
    public boolean isCartEmpty() {

        int count = getCartItemCount();

        boolean empty = count == 0;

        log.info("Cart empty status: {}", empty);

        return empty;
    }

    /**
     * Checks whether product exists in cart.
     */
    public boolean isProductDisplayed(String productName) {

        validateProductName(productName);

        List<WebElement> items = driver.findElements(
                By.cssSelector("div.cart_item[data-test='inventory-item']")
        );

        log.debug(
                "Searching for product '{}' in {} cart item(s)",
                productName,
                items.size()
        );

        for (WebElement cartItem : items) {

            WebElement nameElement = cartItem.findElement(
                    By.cssSelector("[data-test='inventory-item-name']")
            );

            String actualProductName = nameElement.getText().trim();

            log.debug("Cart product found: {}", actualProductName);

            if (actualProductName.equalsIgnoreCase(productName.trim())) {

                log.info(
                        "Product '{}' is displayed in cart",
                        productName
                );

                return true;
            }
        }

        log.warn(
                "Product '{}' was not found in cart",
                productName
        );

        return false;
    }

    // ============================================================
    // Product Name
    // ============================================================

    /**
     * Returns product name from cart.
     */
    public String getProductName(String productName) {

        validateProductName(productName);

        WebElement cartItem = getCartItem(productName);

        String actualName = cartItem
                .findElement(By.cssSelector(
                        "[data-test='inventory-item-name']"))
                .getText()
                .trim();

        log.info(
                "Product name retrieved from cart: {}",
                actualName
        );

        return actualName;
    }

    // ============================================================
    // Product Description
    // ============================================================

    /**
     * Returns product description from cart.
     */
    public String getProductDescription(String productName) {

        validateProductName(productName);

        WebElement cartItem = getCartItem(productName);

        String description = cartItem
                .findElement(By.cssSelector(
                        "[data-test='inventory-item-desc']"))
                .getText()
                .trim();

        log.info(
                "Product description retrieved for '{}': {}",
                productName,
                description
        );

        return description;
    }

    /**
     * Checks whether product description is displayed.
     */
    public boolean isProductDescriptionDisplayed(String productName) {

        validateProductName(productName);

        try {

            WebElement cartItem = getCartItem(productName);

            WebElement description = cartItem.findElement(
                    By.cssSelector("[data-test='inventory-item-desc']")
            );

            boolean displayed =
                    description.isDisplayed()
                    && !description.getText().trim().isEmpty();

            log.debug(
                    "Product description displayed for '{}': {}",
                    productName,
                    displayed
            );

            return displayed;

        } catch (NoSuchElementException | StaleElementReferenceException e) {

            log.warn(
                    "Product description is not displayed for '{}'",
                    productName
            );

            return false;
        }
    }

    // ============================================================
    // Product Price
    // ============================================================

    /**
     * Returns product price exactly as displayed.
     *
     * Example:
     * $29.99
     */
    public String getProductPrice(String productName) {

        validateProductName(productName);

        WebElement cartItem = getCartItem(productName);

        String price = cartItem
                .findElement(By.cssSelector(
                        "[data-test='inventory-item-price']"))
                .getText()
                .trim();

        log.info(
                "Product price for '{}': {}",
                productName,
                price
        );

        return price;
    }

    // ============================================================
    // Product Quantity
    // ============================================================

    /**
     * Returns product quantity from cart.
     *
     * Example:
     * 1
     */
    public int getProductQuantity(String productName) {

        validateProductName(productName);

        WebElement cartItem = getCartItem(productName);

        String quantityText = cartItem
                .findElement(By.cssSelector(
                        "[data-test='item-quantity']"))
                .getText()
                .trim();

        int quantity;

        try {

            quantity = Integer.parseInt(quantityText);

        } catch (NumberFormatException e) {

            log.error(
                    "Unable to parse quantity '{}' for product '{}'",
                    quantityText,
                    productName
            );

            throw new IllegalStateException(
                    "Invalid quantity '" + quantityText
                    + "' for product '" + productName + "'",
                    e
            );
        }

        log.info(
                "Product quantity for '{}': {}",
                productName,
                quantity
        );

        return quantity;
    }

    // ============================================================
    // Remove Product
    // ============================================================

    /**
     * Checks whether Remove button is displayed for a product.
     */
    public boolean isRemoveButtonDisplayed(String productName) {

        validateProductName(productName);

        try {

            WebElement cartItem = getCartItem(productName);

            WebElement removeButton = cartItem.findElement(
                    By.cssSelector("button[data-test^='remove-']")
            );

            boolean displayed = removeButton.isDisplayed();

            log.debug(
                    "Remove button displayed for '{}': {}",
                    productName,
                    displayed
            );

            return displayed;

        } catch (NoSuchElementException | StaleElementReferenceException e) {

            log.warn(
                    "Remove button is not displayed for '{}'",
                    productName
            );

            return false;
        }
    }

    /**
     * Removes a product from cart.
     */
    public void removeProduct(String productName) {

        validateProductName(productName);

        WebElement cartItem = getCartItem(productName);

        WebElement removeButton = cartItem.findElement(
                By.cssSelector("button[data-test^='remove-']")
        );

        log.info(
                "Removing product from cart: {}",
                productName
        );

        click(removeButton);

        log.info(
                "Product removed successfully from cart: {}",
                productName
        );
    }

    // ============================================================
    // Cart Badge
    // ============================================================

    /**
     * Checks whether cart badge is displayed.
     */
    public boolean isCartBadgeDisplayed() {

        try {

            List<WebElement> badges = driver.findElements(
                    By.cssSelector(".shopping_cart_badge")
            );

            boolean displayed =
                    !badges.isEmpty()
                    && badges.get(0).isDisplayed();

            log.debug(
                    "Shopping cart badge displayed: {}",
                    displayed
            );

            return displayed;

        } catch (NoSuchElementException | StaleElementReferenceException e) {

            log.debug("Shopping cart badge is not displayed");

            return false;
        }
    }

    /**
     * Returns cart badge count.
     */
    public int getCartBadgeCount() {

        if (!isCartBadgeDisplayed()) {

            log.info("Cart badge is not displayed. Returning count 0");

            return 0;
        }

        String badgeText = shoppingCartBadge
                .getText()
                .trim();

        try {

            int count = Integer.parseInt(badgeText);

            log.info("Shopping cart badge count: {}", count);

            return count;

        } catch (NumberFormatException e) {

            log.error(
                    "Invalid cart badge value: {}",
                    badgeText
            );

            throw new IllegalStateException(
                    "Invalid shopping cart badge value: " + badgeText,
                    e
            );
        }
    }

    // ============================================================
    // Navigation Buttons
    // ============================================================

    /**
     * Clicks Continue Shopping.
     */
    public void clickContinueShopping() {

        log.info("Clicking Continue Shopping button");

        click(continueShoppingButton);

        log.info("Continue Shopping clicked successfully");
    }

    /**
     * Clicks Checkout.
     */
    public void clickCheckout() {

        log.info("Clicking Checkout button");

        click(checkoutButton);

        log.info("Checkout clicked successfully");
    }

    /**
     * Checks whether Continue Shopping button is displayed.
     */
    public boolean isContinueShoppingDisplayed() {

        try {

            boolean displayed =
                    continueShoppingButton.isDisplayed();

            log.debug(
                    "Continue Shopping button displayed: {}",
                    displayed
            );

            return displayed;

        } catch (NoSuchElementException | StaleElementReferenceException e) {

            return false;
        }
    }

    /**
     * Checks whether Checkout button is displayed.
     */
    public boolean isCheckoutButtonDisplayed() {

        try {

            boolean displayed =
                    checkoutButton.isDisplayed();

            log.debug(
                    "Checkout button displayed: {}",
                    displayed
            );

            return displayed;

        } catch (NoSuchElementException | StaleElementReferenceException e) {

            return false;
        }
    }

    // ============================================================
    // Refresh
    // ============================================================

    /**
     * Refreshes cart page.
     */
    public void refreshPage() {

        log.info("Refreshing Cart page");

        driver.navigate().refresh();

        log.info("Cart page refreshed successfully");
    }

    // ============================================================
    // Private Helper Methods
    // ============================================================

    /**
     * Finds a cart item by product name.
     */
    private WebElement getCartItem(String productName) {

        List<WebElement> items = driver.findElements(
                By.cssSelector("div.cart_item[data-test='inventory-item']")
        );

        for (WebElement cartItem : items) {

            WebElement nameElement = cartItem.findElement(
                    By.cssSelector("[data-test='inventory-item-name']")
            );

            String actualProductName =
                    nameElement.getText().trim();

            if (actualProductName.equalsIgnoreCase(productName.trim())) {

                return cartItem;
            }
        }

        throw new IllegalArgumentException(
                "Product '" + productName + "' was not found in the cart"
        );
    }

    /**
     * Validates product name.
     */
    private void validateProductName(String productName) {

        if (productName == null || productName.trim().isEmpty()) {

            throw new IllegalArgumentException(
                    "Product name must not be null or blank"
            );
        }
    }
}
```

### Important change

I specifically changed the selectors to match your HTML:

```java
By.cssSelector("[data-test='inventory-item-name']")
```

instead of relying on:

```java
By.cssSelector(".inventory_item_name")
```

Both can work, but the `data-test` attributes shown in your DOM are very good automation locators.

Your cart item locator is now:

```java
div.cart_item[data-test='inventory-item']
```

So:

```java
getCartItemCount()
```

will correctly find:

```html
<div class="cart_item" data-test="inventory-item">
```

---

## 2. Your exact HTML → Selenium mapping

Your element:

```html
<div class="cart_item" data-test="inventory-item">
```

maps to:

```java
@FindBy(css = "div.cart_item[data-test='inventory-item']")
private List<WebElement> cartItems;
```

Quantity:

```html
<div class="cart_quantity" data-test="item-quantity">1</div>
```

maps to:

```java
By.cssSelector("[data-test='item-quantity']")
```

Product name:

```html
<div class="inventory_item_name"
     data-test="inventory-item-name">
    Sauce Labs Backpack
</div>
```

maps to:

```java
By.cssSelector("[data-test='inventory-item-name']")
```

Description:

```html
<div class="inventory_item_desc"
     data-test="inventory-item-desc">
```

maps to:

```java
By.cssSelector("[data-test='inventory-item-desc']")
```

Price:

```html
<div class="inventory_item_price"
     data-test="inventory-item-price">
    $29.99
</div>
```

maps to:

```java
By.cssSelector("[data-test='inventory-item-price']")
```

Remove:

```html
<button
    class="btn btn_secondary btn_small cart_button"
    data-test="remove-sauce-labs-backpack"
    id="remove-sauce-labs-backpack">
    Remove
</button>
```

maps to:

```java
By.cssSelector("button[data-test^='remove-']")
```

The `^=` means **starts with**, so it works for all products:

```text
remove-sauce-labs-backpack
remove-sauce-labs-bike-light
remove-sauce-labs-bolt-t-shirt
remove-sauce-labs-fleece-jacket
remove-sauce-labs-onesie
remove-test.allthethings-t-shirt-red
```

---

## 3. Why your previous count was `0`

Your previous failure was:

```text
expected [1] but found [0]
```

With the HTML you supplied, this selector:

```java
@FindBy(css = ".cart_item")
private List<WebElement> cartItems;
```

**should actually find the item**, because the element has:

```html
class="cart_item"
```

However, I recommend changing it to:

```java
@FindBy(css = "div.cart_item[data-test='inventory-item']")
private List<WebElement> cartItems;
```

And, more importantly, for count verification I recommend dynamically locating the elements:

```java
public int getCartItemCount() {

    int count = driver.findElements(
            By.cssSelector("div.cart_item[data-test='inventory-item']")
    ).size();

    log.info("Number of products currently in cart: {}", count);

    return count;
}
```

This avoids relying on a potentially stale PageFactory list.

---

## 4. Your `CartAssertions.java`

Your existing assertion pattern can remain almost unchanged.

```java
package assertions;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.testng.Assert;

import pages.CartPage;
import utils.WaitUtils;

public class CartAssertions {

    private static final Logger log =
            LoggerFactory.getLogger(CartAssertions.class);

    private final CartPage cartPage;

    public CartAssertions(CartPage cartPage) {

        if (cartPage == null) {
            throw new IllegalArgumentException(
                    "CartPage must not be null");
        }

        this.cartPage = cartPage;

        log.info("CartAssertions initialized successfully");
    }

    // ============================================================
    // URL
    // ============================================================

    public void verifyShoppingCartUrlContains(String expectedUrlPart) {

        log.info(
                "Verifying cart URL contains: {}",
                expectedUrlPart
        );

        String actualUrl = cartPage.getCurrentUrl();

        Assert.assertTrue(
                actualUrl.contains(expectedUrlPart),
                "Cart URL does not contain expected value. "
                        + "Expected: " + expectedUrlPart
                        + ", Actual: " + actualUrl
        );

        log.info("Cart URL verification passed");
    }

    // ============================================================
    // Cart List
    // ============================================================

    public void verifyCartListDisplayed() {

        log.info("Verifying cart list is displayed");

        Assert.assertTrue(
                cartPage.isCartListDisplayed(),
                "Cart list should be displayed"
        );

        log.info("Cart list verification passed");
    }

    // ============================================================
    // Cart Item Count
    // ============================================================

    public void verifyCartItemCount(int expectedCount) {

        log.info(
                "Verifying cart item count. Expected: {}",
                expectedCount
        );

        int actualCount = cartPage.getCartItemCount();

        Assert.assertEquals(
                actualCount,
                expectedCount,
                "Cart item count is incorrect"
        );

        log.info(
                "Cart item count verification passed. Count: {}",
                actualCount
        );
    }

    // ============================================================
    // Empty Cart
    // ============================================================

    public void verifyCartIsEmpty() {

        log.info("Verifying cart is empty");

        Assert.assertTrue(
                cartPage.isCartEmpty(),
                "Cart should be empty"
        );

        log.info("Cart empty verification passed");
    }

    // ============================================================
    // Product
    // ============================================================

    public void verifyProductDisplayed(String productName) {

        log.info(
                "Verifying product is displayed in cart: {}",
                productName
        );

        Assert.assertTrue(
                cartPage.isProductDisplayed(productName),
                "Product '" + productName
                        + "' should be displayed in cart"
        );

        log.info(
                "Product verification passed: {}",
                productName
        );
    }

    // ============================================================
    // Product Name
    // ============================================================

    public void verifyProductName(
            String productName,
            String expectedProductName) {

        log.info(
                "Verifying product name. Product: {}, Expected: {}",
                productName,
                expectedProductName
        );

        String actualProductName =
                cartPage.getProductName(productName);

        Assert.assertEquals(
                actualProductName,
                expectedProductName,
                "Product name is incorrect"
        );

        log.info("Product name verification passed");
    }

    // ============================================================
    // Product Price
    // ============================================================

    public void verifyProductPrice(
            String productName,
            String expectedPrice) {

        log.info(
                "Verifying product price. Product: {}, Expected: {}",
                productName,
                expectedPrice
        );

        String actualPrice =
                cartPage.getProductPrice(productName);

        Assert.assertEquals(
                actualPrice,
                expectedPrice,
                "Product price is incorrect"
        );

        log.info("Product price verification passed");
    }

    // ============================================================
    // Product Quantity
    // ============================================================

    public void verifyProductQuantity(
            String productName,
            int expectedQuantity) {

        log.info(
                "Verifying product quantity. Product: {}, Expected: {}",
                productName,
                expectedQuantity
        );

        int actualQuantity =
                cartPage.getProductQuantity(productName);

        Assert.assertEquals(
                actualQuantity,
                expectedQuantity,
                "Product quantity is incorrect"
        );

        log.info("Product quantity verification passed");
    }

    // ============================================================
    // Product Description
    // ============================================================

    public void verifyProductDescriptionDisplayed(
            String productName) {

        log.info(
                "Verifying product description for: {}",
                productName
        );

        Assert.assertTrue(
                cartPage.isProductDescriptionDisplayed(productName),
                "Product description should be displayed for '"
                        + productName + "'"
        );

        log.info(
                "Product description verification passed"
        );
    }

    // ============================================================
    // Remove Button
    // ============================================================

    public void verifyRemoveButtonDisplayed(
            String productName) {

        log.info(
                "Verifying Remove button for: {}",
                productName
        );

        Assert.assertTrue(
                cartPage.isRemoveButtonDisplayed(productName),
                "Remove button should be displayed for '"
                        + productName + "'"
        );

        log.info(
                "Remove button verification passed"
        );
    }

    // ============================================================
    // Cart Badge
    // ============================================================

    public void verifyCartBadgeDisplayed() {

        log.info("Verifying cart badge is displayed");

        Assert.assertTrue(
                cartPage.isCartBadgeDisplayed(),
                "Cart badge should be displayed"
        );

        log.info("Cart badge verification passed");
    }

    public void verifyCartBadgeNotDisplayed() {

        log.info("Verifying cart badge is not displayed");

        Assert.assertFalse(
                cartPage.isCartBadgeDisplayed(),
                "Cart badge should not be displayed"
        );

        log.info("Cart badge not displayed verification passed");
    }

    public void verifyCartBadgeCount(int expectedCount) {

        log.info(
                "Verifying cart badge count. Expected: {}",
                expectedCount
        );

        int actualCount =
                cartPage.getCartBadgeCount();

        Assert.assertEquals(
                actualCount,
                expectedCount,
                "Cart badge count is incorrect"
        );

        log.info(
                "Cart badge count verification passed. Count: {}",
                actualCount
        );
    }
}
```

---

## 5. Most important test

With the exact HTML you supplied, this scenario should now work:

```gherkin
Scenario: Verify one product is displayed in cart

    Given the user is on the SauceDemo login page

    When the user logs in with username "standard_user" and password "secret_sauce"

    And the user adds "Sauce Labs Backpack" to the cart

    And the user opens the shopping cart

    Then the shopping cart URL should contain "cart.html"

    And the cart list should be displayed

    And the cart should contain 1 product

    And the product "Sauce Labs Backpack" should be displayed in the cart

    And the price of "Sauce Labs Backpack" should be "$29.99"

    And the quantity of "Sauce Labs Backpack" should be 1

    And the product description should be displayed for "Sauce Labs Backpack"

    And the Remove button should be displayed for "Sauce Labs Backpack"
```

### Expected result

Your DOM:

```html
<div class="cart_item" data-test="inventory-item">
```

will be found by:

```java
div.cart_item[data-test='inventory-item']
```

Therefore:

```java
getCartItemCount()
```

should return:

```text
1
```

and:

```java
isProductDisplayed("Sauce Labs Backpack")
```

should return:

```text
true
```

and:

```java
getProductPrice("Sauce Labs Backpack")
```

should return:

```text
$29.99
```

and:

```java
getProductQuantity("Sauce Labs Backpack")
```

should return:

```text
1
```

This also matches the commonly documented SauceDemo product/cart data, including **Sauce Labs Backpack = $29.99**.

**One additional recommendation:** if you still get `found [0]` after this change, the next thing to inspect is **not the locator**—it is whether the product was actually added before `cart.html` was opened. In that case, log the cart URL, cart badge, and `getCartItemCount()` immediately after opening the cart.
    